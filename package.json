{
  "name": "club",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "npm ci --prefix client && npm run build --prefix client",
    "build:client": "npm run build --prefix client",
    "dev": "concurrently -n server,client -c green,magenta \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "dev:server": "npm run dev --prefix server",
    "dev:client": "npm run dev --prefix client",
    "pages:login": "npm run pages:login --prefix client",
    "pages:deploy": "npm run pages:deploy --prefix client",
    "render:login": "render login",
    "render:validate": "render blueprints validate render.yaml",
    "render:services": "render services list -o text --confirm",
    "render:create": "pwsh -NoProfile -File scripts/render-create-api.ps1",
    "render:env": "node scripts/push-render-env.mjs",
    "render:rotate-admin": "node scripts/rotate-render-admin-password.mjs",
    "render:deploy": "render deploys create",
    "render:logs": "render logs"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
